# ローカル(deno task dev)用の環境変数。コピーして .env を作る:
#   cp .env.example .env
# .env は .gitignore 済み（コミットしないこと）。本番は Deno Deploy のダッシュボードで設定する。

SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXXXX/YYYYY/ZZZZZ

# 以下は任意（未設定なら東京都／東京地方）
# AREA_CODE=130000
# AREA_NAME=東京都
# FORECAST_AREA_INDEX=0
